Timeline Gallery

This is a perfect consolidation. Below is the final, unified code block containing the HTML, CSS, and JS logic exactly as we discussed. It perfectly mirrors the entry mechanics and UI layout of the reference site, completely primed for your custom assets.

You can copy this entire block directly into your `resources.txt` file for your AI agents to use during the final build phase.

---

### 1. Unified Structure (The Blueprint)

Save the following text into your `resources.txt` file. When you are ready to link everything, you will tell your AI agent: *"Read `resources.txt` and generate the `index.html`, `css/style.css`, and `js/main.js` based on these exact specifications."*

```text
=========================================
PROJECT RESOURCE BLUEPRINT: THE SPIRAL UI
=========================================
Theme: Betrayal / Dark Paper / Regret
Tech Stack: HTML, CSS Variables, SVG, GSAP (ScrollTrigger), Three.js

--- [1] index.html ---
<body>
    <audio id="bg-audio" loop>
        <source src="assets/audio/bg-song.mp3" type="audio/mpeg">
    </audio>

    <!-- ENTRY GATE -->
    <div id="entry-screen">
        <div class="entry-content">
            <button id="btn-sound" class="entry-btn">Enter with sound</button>
            <button id="btn-no-sound" class="entry-btn">Enter without sound</button>
        </div>
    </div>

    <!-- UI LAYER (Z-Index: 999) -->
    <div id="ui-layer">
        <!-- Top Left -->
        <div id="top-left-logo">
            <img src="assets/images/custom-sticker.png" alt="Logo">
        </div>

        <!-- Top Center -->
        <div id="view-toggle">
            <button class="active">spiral</button>
            <span class="dot">•</span>
            <button>list</button>
        </div>

        <!-- Top Right -->
        <div id="top-right-menu">
            <button id="menu-btn">menu •</button>
        </div>

        <!-- Bottom Left (SVG Curved Text) -->
        <div id="bottom-left-badge">
            <svg viewBox="0 0 100 100" width="200" height="200">
                <defs>
                    <path id="circlePath" d="M 50, 50 m -40, 0 a 40,40 0 1,1 80,0 a 40,40 0 1,1 -80,0" />
                </defs>
                <text font-size="9" font-family="monospace" fill="var(--text-color)" letter-spacing="1.5">
                    <textPath href="#circlePath">
                        betrayal • regret • 2026 • betrayal • regret • 2026 • 
                    </textPath>
                </text>
            </svg>
        </div>

        <!-- Bottom Right -->
        <div id="bottom-right-audio">
            <button id="audio-toggle-btn">🔊</button>
        </div>
    </div>

    <!-- 3D CANVAS LAYER (Z-Index: 1) -->
    <main id="main-content">
        <canvas id="scroll-canvas"></canvas>
    </main>

    <!-- Scripts -->
    <script src="https://cdnjs.cloudflare.com/ajax/libs/three.js/r128/three.min.js"></script>
    <script src="https://cdnjs.cloudflare.com/ajax/libs/gsap/3.12.2/gsap.min.js"></script>
    <script src="https://cdnjs.cloudflare.com/ajax/libs/gsap/3.12.2/ScrollTrigger.min.js"></script>
    <script src="js/main.js"></script>
</body>

--- [2] css/style.css ---
:root {
    --bg-color: #050505; /* Pitch black / dark paper base */
    --text-color: #e0e0e0;
    --accent-color: #333333;
    --font-primary: 'Courier New', monospace; /* For that raw, editorial feel */
}

body, html {
    margin: 0; padding: 0; width: 100%; height: 100%;
    overflow: hidden; background-color: var(--bg-color);
    color: var(--text-color); font-family: var(--font-primary);
}

/* Entry Gate */
#entry-screen {
    position: fixed; top: 0; left: 0; width: 100vw; height: 100vh;
    background-color: var(--bg-color); z-index: 9999;
    display: flex; justify-content: center; align-items: center;
    transition: transform 1.2s cubic-bezier(0.77, 0, 0.175, 1), opacity 1s ease;
}
#entry-screen.hide { transform: translateY(-100%); opacity: 0; pointer-events: none; }
.entry-btn {
    background: transparent; border: 1px solid var(--accent-color); color: var(--text-color);
    padding: 15px 30px; margin: 10px; cursor: pointer; border-radius: 4px;
    font-family: inherit; text-transform: lowercase; transition: 0.3s;
}
.entry-btn:hover { background: var(--text-color); color: var(--bg-color); }

/* UI Layer */
#ui-layer {
    position: fixed; top: 0; left: 0; width: 100vw; height: 100vh; z-index: 999;
    pointer-events: none; opacity: 0; transition: opacity 1s ease 0.5s;
}
body.site-loaded #ui-layer { opacity: 1; }
#ui-layer button, #ui-layer #bottom-left-badge { pointer-events: auto; cursor: pointer; }

/* Positioning */
#top-left-logo { position: absolute; top: 30px; left: 30px; }
#top-left-logo img { width: 50px; opacity: 0.8; filter: grayscale(100%); }

#view-toggle { position: absolute; top: 30px; left: 50%; transform: translateX(-50%); display: flex; gap: 10px; }
#view-toggle button { background: none; border: none; color: var(--accent-color); font-family: inherit; }
#view-toggle button.active { color: var(--text-color); }

#top-right-menu button { position: absolute; top: 30px; right: 30px; background: none; border: none; color: var(--text-color); font-family: inherit; text-decoration: underline; }

/* SVG Rotating Badge */
#bottom-left-badge {
    position: absolute; bottom: -50px; left: -50px; width: 200px; height: 200px;
    animation: spinText 20s linear infinite; transform-origin: center;
}
@keyframes spinText { 100% { transform: rotate(360deg); } }

#bottom-right-audio button { position: absolute; bottom: 30px; right: 30px; background: none; border: none; font-size: 24px; opacity: 0.7; }

/* Canvas Layer */
#main-content { position: absolute; top: 0; left: 0; width: 100%; height: 100%; z-index: 1; }

--- [3] js/main.js ---
document.addEventListener("DOMContentLoaded", () => {
    const entryScreen = document.getElementById('entry-screen');
    const bgAudio = document.getElementById('bg-audio');
    const audioToggleBtn = document.getElementById('audio-toggle-btn');
    let isMuted = false;

    function unlockSite(playAudio) {
        if (playAudio) {
            bgAudio.volume = 0.4;
            bgAudio.play().catch(e => console.log("Audio blocked"));
            isMuted = false; audioToggleBtn.innerText = "🔊";
        } else {
            isMuted = true; audioToggleBtn.innerText = "🔇";
        }
        
        entryScreen.classList.add('hide');
        document.body.classList.add('site-loaded');

        setTimeout(() => {
            entryScreen.style.display = 'none';
            initThreeJSSpiral(); // Initialize 3D Engine
        }, 1200);
    }

    document.getElementById('btn-sound').addEventListener('click', () => unlockSite(true));
    document.getElementById('btn-no-sound').addEventListener('click', () => unlockSite(false));

    audioToggleBtn.addEventListener('click', () => {
        if (isMuted) { bgAudio.play(); isMuted = false; audioToggleBtn.innerText = "🔊"; } 
        else { bgAudio.pause(); isMuted = true; audioToggleBtn.innerText = "🔇"; }
    });
});

function initThreeJSSpiral() {
    const canvas = document.getElementById('scroll-canvas');
    const scene = new THREE.Scene();
    const renderer = new THREE.WebGLRenderer({ canvas: canvas, alpha: true, antialias: true });
    renderer.setSize(window.innerWidth, window.innerHeight);
    const camera = new THREE.PerspectiveCamera(75, window.innerWidth / window.innerHeight, 0.1, 1000);
    camera.position.z = 15;

    const spiralGroup = new THREE.Group();
    scene.add(spiralGroup);

    const totalImages = 15; const radius = 8; const heightGap = 0.7;
    const geometry = new THREE.PlaneGeometry(3, 4.5); // Vertical portrait aspect ratio

    for (let i = 0; i < totalImages; i++) {
        const material = new THREE.MeshBasicMaterial({ color: 0x222222, side: THREE.DoubleSide });
        const plane = new THREE.Mesh(geometry, material);
        const angle = (i / totalImages) * Math.PI * 2 * 2; 

        plane.position.x = Math.sin(angle) * radius;
        plane.position.z = Math.cos(angle) * radius;
        plane.position.y = (i * -heightGap) + (totalImages * heightGap / 2);
        plane.rotation.y = angle; 
        
        spiralGroup.add(plane);
    }

    document.body.style.height = "6000px"; // Scroll depth
    gsap.registerPlugin(ScrollTrigger);

    gsap.to(spiralGroup.rotation, {
        y: Math.PI * 4,
        ease: "none",
        scrollTrigger: { trigger: document.body, start: "top top", end: "bottom bottom", scrub: 1 }
    });

    gsap.to(spiralGroup.position, {
        y: totalImages * heightGap,
        ease: "none",
        scrollTrigger: { trigger: document.body, start: "top top", end: "bottom bottom", scrub: 1 }
    });

    function animate() { requestAnimationFrame(animate); renderer.render(scene, camera); }
    animate();

    window.addEventListener('resize', () => {
        camera.aspect = window.innerWidth / window.innerHeight;
        camera.updateProjectionMatrix();
        renderer.setSize(window.innerWidth, window.innerHeight);
    });
}

```


=========================================
INTEGRATION PROCESS: ADDING YOUR CONTENT
=========================================

STEP 1: FOLDER POPULATION
- Drop your background song into `assets/audio/` and name it `bg-song.mp3`.
- Drop your top-left logo/sticker into `assets/images/` and name it `custom-sticker.png`. (actually not needed ignore this, no need for this)
- Drop your timeline/gallery photos into `assets/images/`. Name them sequentially (e.g., `1.jpg`, `2.jpg`, ..., `15.jpg`).

STEP 2: UPDATE THE ROTATING TEXT (SVG)
- Open `index.html`.
- Locate the `<textPath>` inside the `#bottom-left-badge`.
- Change "betrayal • regret • 2026 •" to whatever custom message you want. Keep the spacing consistent so it connects perfectly in a circle.

STEP 3: UPDATE THE CSS THEME
- Open `css/style.css`.
- Go to the `:root` variables at the top.
- Replace `--bg-color`, `--text-color`, and `--font-primary` with the exact hex codes and fonts you chose in Google Stitch.

STEP 4: MAP YOUR PHOTOS TO THE 3D SPIRAL
- Open `js/main.js`.
- Locate the `initThreeJSSpiral()` function.
- Find this line: 
  `const material = new THREE.MeshBasicMaterial({ color: 0x222222, side: THREE.DoubleSide });`
- REPLACE IT with these two lines to load your actual images instead of grey boxes:
  
  // Create this outside the loop first:
  const textureLoader = new THREE.TextureLoader();
  
  // Put this inside the loop:
  const texture = textureLoader.load(`assets/images/${i + 1}.jpg`);
  const material = new THREE.MeshBasicMaterial({ map: texture, side: THREE.DoubleSide });

STEP 5: INJECT STITCH UI
- When you design additional overlay elements in Stitch (like the menu pop-out or list view), place their HTML inside the `<div id="ui-layer">` in `index.html`.